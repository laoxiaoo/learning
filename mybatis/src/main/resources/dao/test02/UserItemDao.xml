<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiao.dualDataSource.dao2.ReadStudentDao">
    <insert id="insert">
        insert into student(name) values('读库新增')
    </insert>

    <select id="getStudent" resultType="java.lang.Object">
        select * from student
    </select>
</mapper>